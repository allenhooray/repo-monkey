{
  "name": "repo-monkey",
  "version": "1.0.0",
  "description": "Chrome extension that manages userscripts via GitHub",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && echo \"Use 'npm run build' to build the extension\""
  }
}
